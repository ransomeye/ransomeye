# Phase 6.0 — AI execution gate (historical)

> **Superseded.** Mishka Phase-1 uses **signed `integrity.manifest`** + **`integrity.sig`** verified by **`ransomeye-core`** and the installer; see [`PHASE_6_3_CRYPTO_TRUST.md`](PHASE_6_3_CRYPTO_TRUST.md).

Earlier revisions described a Core-spawned interpreter model. That design is not used in the current three-service slice (PostgreSQL + Core + Nginx).
