local   all             postgres                               peer
# TLS 1.3 + SCRAM + client certificate (verify-ca). Server/client trust: /opt/ransomeye/core/certs/ (installer PKI only).
hostssl all             all                    127.0.0.1/32             scram-sha-256 clientcert=verify-ca
hostnossl all           all                    127.0.0.1/32             reject
host    all             all                    0.0.0.0/0                reject
host    all             all                    ::0/0                    reject
